# what IP to bind to.  127.0.0.1 by default, 0.0.0.0 might be a good choice
HOST = "127.0.0.1"

# what port to bind to
PORT = 5000

# sqlalchemy db connection uri
SQLALCHEMY_DATABASE_URI = "sqlite:////tmp/userapi.db"

# Any other flask config values can go here, see http://flask.pocoo.org/docs/0.10/config/
DEBUG = True
